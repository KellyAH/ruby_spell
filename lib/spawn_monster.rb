# handle generating the correct monster for the correct stage the player is in
class SpawnMonster
  def show_monster(monster)
    puts "a #{monster.class} monster called the #{monster} has appeared!"
  end
end
